name: Fix commercial create source

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/fix-commercial-create.yml

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Update source
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path = Path('src/app/menu/new/page.tsx')
          text = path.read_text(encoding='utf-8-sig')

          def replace_once(old: str, new: str, label: str) -> None:
              global text
              if